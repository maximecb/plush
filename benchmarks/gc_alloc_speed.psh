// An object with no fields
class C
{
    init(self)
    {
    }
}

for (let var i = 0; i < 100; ++i)
{
    // Allocate 1M tiny objects
    for (let var i = 0; i < 1_000_000; ++i)
    {
        C();
    }

    // Trigger a GC collection
    $vm_gc_collect();
}
