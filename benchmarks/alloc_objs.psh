let start_time = $time_current_ms();

class Foo
{
    init(self)
    {
        self.x = 1;
        self.y = 2;
        self.z = 3;
        self.w = 4;
    }
}

// Note: this will run out of memory and trigger GC
for (let var i = 0; i < 100_000_000; i = i + 1)
{
    Foo();
}

let end_time = $time_current_ms();
$println(end_time - start_time);
