let count = 15_000_000;

let t0 = $time_current_ms();

for (let var i = 0; i < count; ++i) {
    $time_current_ms();
}

let t1 = $time_current_ms();
let dt = (t1 - t0) / 1000;
let calls_per_s = (count / dt).floor();

$println('Host calls per sec: ' + calls_per_s.to_s());
