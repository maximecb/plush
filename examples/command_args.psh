$println("Number of command-line arguments: " + $cmd_num_args().to_s());

for (let var i = 0; i < $cmd_num_args(); ++i)
{
    let arg = $cmd_get_arg(i);
    $println("args[" + i.to_s() + "] = " + arg);
}
