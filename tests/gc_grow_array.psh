$vm_shrink_heap(256_000);

let a = [];

for (let var i = 0; i < 3_000_000; ++i)
{
    a.push(1);
}

let var sum = 0;
for (let var i = 0; i < a.len; ++i)
{
    sum = sum + a[i];
}

assert(a.len == 3_000_000);
assert(sum == a.len);
