// dag2 will import dag3 as well, so dag3 is imported twice
from ./import_dag2 import f2;

// We can have multiple imports from the same unit
// in separate import directives if desired
from ./import_dag3 import f3;
from ./import_dag3 import f4;

let var g1 = 1;

assert(f2() == 2);
assert(f3() == 3);
assert(f4() == 4);
assert(g1 == 1);
