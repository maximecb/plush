$vm_shrink_heap(256_000);

let var str = nil;

for (let var n = 0; n < 500; ++n)
{
    str = "";

    for (let var i = 0; i < 800; ++i)
    {
        str = str + "foo";
    }

    assert(str.len == 2400);
}
