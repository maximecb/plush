let d1 = { a:1 };
assert(d1.has('b') == false);

assert({}.has('a') == false);
assert({}.has(' ') == false);
assert({ a:1 }.has('a') == true);

let d = { a: 1};
for (let var i = 0; i < 200; ++i)
{
    d[i.to_s()] = i;
}
assert(d.has('a'));
assert(d.has('100'));
assert(!d.has('500'));
assert(!d.has(' '));
